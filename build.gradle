plugins {
    id 'java'
    id 'application'
}

mainClassName = 'jwikistarter.JwikiExample'

dependencies {
    compile 'fastily:jwiki:1.6.0'
}

repositories {
    mavenLocal()
    jcenter()
}

task wrapper(type: Wrapper) {
    gradleVersion = '4.7'
}
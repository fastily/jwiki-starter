plugins {
    id 'java'
    id 'application'
}

mainClassName = 'jwikistarter.JwikiExample'

dependencies {
    compile 'fastily:jwiki:1.6.0'
}

repositories {
    mavenLocal()
    jcenter()
}

wrapper {
    gradleVersion = '4.8'
}